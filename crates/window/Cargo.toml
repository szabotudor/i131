[package]
name = "window131"
version = "0.1.0"
edition.workspace = true

[dependencies]
math131.workspace = true

glfw = { version = "0.62.0", optional = true, default-features = true, features = [ "vulkan", "image", "raw-window-handle-v0-6" ] }
raw-window-handle = { version = "0.6.2", optional = true }

thiserror.workspace = true

[features]
GLFW = [ "glfw", "raw-window-handle" ]
