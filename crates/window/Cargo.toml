[package]
name = "window131"
version = "0.1.0"
edition.workspace = true

[dependencies]
math131.workspace = true

glfw = { version = "0.61.0", optional = true, features = [ "vulkan", "image" ] }

thiserror.workspace = true

[features]
GLFW = [ "glfw" ]
