[package]
name = "renderer131"
version = "0.1.0"
edition.workspace = true

[dependencies]
backends.workspace = true
vulkan-backend = { workspace = true, optional = true }

anyhow.workspace = true

[features]
vulkan = [ "dep:vulkan-backend" ]

# TODO: No backend should be ebabled by default, unless it's supported on the platform being built
default = ["vulkan"]
