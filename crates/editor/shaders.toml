[config]
always_rebuild = false

[shaders]
"default.vert" = { backend = "Vulkan", stage = "Vertex", file = "src/shaders/default.vert" }
"default.frag" = { backend = "Vulkan", stage = "Pixel", file = "src/shaders/default.frag" }
