[package]
name = "editor131"
version = "0.1.0"
edition.workspace = true

[dependencies]
anyhow.workspace = true

[target.'cfg(any(target_os = "linux", target_os = "windows"))'.dependencies]
engine131 = { workspace = true, features = [ "vulkan", "GLFW" ] }
